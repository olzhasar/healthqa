version: "3"

services:
  web:
    build: ./backend

  worker:
    build: ./backend

  nginx:
    build: .
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf.dev:/etc/nginx/conf.d/nginx.conf
